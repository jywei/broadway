<h2>Edit Broadway Play</h2>

<br>

<%= simple_form_for @play, html: { multipart: true } do |f| %>
  <%= f.select :category_id, @categories %>
  <%= f.file_field :play_img %>
  <%= f.input :title, label: "Play Title" %>
  <%= f.input :description %>
  <%= f.input :director %>
  <%= f.button :submit %>
<% end %>
